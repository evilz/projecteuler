@namespace Projecteuler.Components.Problems
@page "/problem/56"
@inherits ProblemBase

@{
    base.BuildRenderTree(__builder);
}

@code {
    protected override int Ref => 56;

    protected override string ProblemTitle => "Powerful Digit Sum";

    protected override string Description => "<p>A googol ($10^{100}$) is a massive number: one followed by one-hundred zeros; $100^{100}$ is almost unimaginably large: one followed by two-hundred zeros. Despite their size, the sum of the digits in each number is only $1$.</p>\n<p>Considering natural numbers of the form, $a^b$, where $a, b \\\\lt 100$, what is the maximum digital sum?</p>";

    protected override string Explanation => "Not implemented yet.";

    protected override long Resolve()
    {
        // TODO: Implement problem 56
        // Powerful Digit Sum
        return 0;
    }
}


